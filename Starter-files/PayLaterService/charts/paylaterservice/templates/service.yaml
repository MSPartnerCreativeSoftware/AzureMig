apiVersion: v1
kind: Service
metadata:
  name: {{ include "PayLaterService.fullname" . }}
  labels:
    {{- include "PayLaterService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "PayLaterService.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "PayLaterService.fullname" . }}-test-connection"
  labels:
    {{- include "PayLaterService.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "PayLaterService.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

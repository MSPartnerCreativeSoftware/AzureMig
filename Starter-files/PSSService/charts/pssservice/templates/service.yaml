apiVersion: v1
kind: Service
metadata:
  name: {{ include "PSSService.fullname" . }}
  labels:
    {{- include "PSSService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "PSSService.selectorLabels" . | nindent 4 }}
